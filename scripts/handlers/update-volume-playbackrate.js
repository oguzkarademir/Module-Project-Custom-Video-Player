/* Get Our Elements (with querySelector)*/

debugger;

const ranges = player.querySelectorAll('.player__slider');

/* In this function we can change the volume
 *  and playback speed using the change-mousemove events 
 */
function updateRangeVolumePlaybackRate() {
    video[this.name] = this.value;
}


/* write your event listeners in event-listeners.js*/